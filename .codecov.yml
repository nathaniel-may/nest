coverage:
  status:
    project:
      default: off
      nest:
        flags: nest
        target: 80%

flags:
  nest:
    paths:
      - src/main/scala/nest
    assume: true
  tests:
    paths:
      - src/test/scala/nest